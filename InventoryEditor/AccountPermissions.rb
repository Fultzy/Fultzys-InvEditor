userinfo = [Alex, 1234]

puts "Please enter a username"
name = gets.chomp
if user? == true
  userinfo.each {|user, password| }
else
  puts "User not found"
end


class User
  @@users == 1
  def add_user(name, p)
    @@name = name
    @@password = password
    @@users += 1
    userinfo.push = name, p
  end
  def delete_user(name, p)
    @@name = user.each {|user, password| user.remove }
  end
end
